name: Build

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  repository_dispatch:
    types: [data-updated]

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Build Tauri app (ARM64)
        run: npm run tauri build -- --target aarch64-apple-darwin

      - name: Build Tauri app (x64)
        run: npm run tauri build -- --target x86_64-apple-darwin

      - name: Rename ARM64 DMG
        run: |
          cd src-tauri/target/aarch64-apple-darwin/release/bundle/dmg
          for f in *_aarch64.dmg; do
            newname="${f/_aarch64.dmg/_arm64.dmg}"
            mv "$f" "$newname"
            echo "Renamed: $f -> $newname"
          done

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-builds
          path: |
            src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Build Tauri app
        run: npm run tauri build -- --verbose
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-builds
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    # 关键修改：允许 repository_dispatch 触发发布
    if: (github.event_name == 'push' && (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')) || github.event_name == 'repository_dispatch'
    steps:
      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: macos-builds
          path: ./artifacts/macos

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: windows-builds
          path: ./artifacts/windows

      - name: List artifacts
        run: find ./artifacts -type f

      - name: Get short SHA
        id: sha
        run: echo "short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          # 为自动构建添加特别的 tag 前缀
          tag_name: ${{ github.event_name == 'repository_dispatch' && format('auto-data-{0}', steps.sha.outputs.short) || format('build-{0}', steps.sha.outputs.short) }}
          name: ${{ github.event_name == 'repository_dispatch' && format('Data Update Build {0}', steps.sha.outputs.short) || format('Build {0}', steps.sha.outputs.short) }}
          files: |
            ./artifacts/macos/**/*.dmg
            ./artifacts/windows/**/*.msi
            ./artifacts/windows/**/*.exe
          generate_release_notes: true
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
